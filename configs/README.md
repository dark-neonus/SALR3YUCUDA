# configs/

Simulation parameter files in INI-style `.cfg` format.

The file `default.cfg` contains a reference 2D configuration with all 3-Yukawa interaction coefficients, grid parameters, solver settings, and output options. See the root `README.md` for a description of each configuration section.
