
This is semester project for Architecture of Computer Systems course, so main focus must be on performance, efficienct, parallel computing and benchmarking, but without losing a focus of topic of project.

Check Annotation.md for more info about directories.

There should be very well done and described analisys of performance and comparasion of our project on different computational systems. Raw data was collected and saved in projects_root/analysis/results. You man need to visualize this data. For this write script to create plots of data. Again, since project if for performance and paralelisation course main emphasis need to be done on this things and things related to course. No collected data should be missing from final report and plots.

Demand and suggestions for figures. Images of result distribution(snapshots) are not necessary to be big and even preffered to be comapct, but still easy to comprehend.
On the other hand plots need to be big and easy readable without zooming for a persong with average sight.

Desired structure of report with short description about info that should be(but not limit info amount, details and even topics) in sections is in ReportStructure.md. If you se some inconvinince between data and report structure(like structure say we run cluster cpu for 1, 2, 4, 32, 64 cores, but we have additional data for 128), prefer data, but if some data is missing notify about it and leave placeholder in report.

It is your responcibility to track images relativity and up-to-dateness. If some image is needed(like snapshot of results specific parameter set with starting conditions) but is not available/presented, you need to specify it in some sort of requirements(and say what name to give to this image if it will be added by me) and leave a placehodler in report. At the start of each prompt you need to check if requested iamges appeared, and if so, remove them from requirements and include(if not yet) in report.

All images/figures used in report should be placced/copied into: docs/report/SALR3YUCUDA/src

Dont use long dashes if they are not strictrly needed(like in the abstract). Try to avoid them and you use them, use minus sign "-" instead.

## Notes

We desided to not use triple-Yukawa potential, and instead use three-Yukawa potential. Comply with it in future and if you see this mistake presented, fix it.