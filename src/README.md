# src/

Main source code directory (pure C).

- **main.c** — Entry point
- **core/** — Config parser, grid initialisation
- **cpu/** — CPU implementations (solver, potential, math)
- **cuda/** — GPU kernels (future — will mirror cpu/)
- **math/** — Specialised math routines (future)
- **utils/** — I/O helpers
