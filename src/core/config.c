/*
 * config.c — Configuration file parser (CPU, pure C)
 *
 * TODO: implement .cfg file reading and SimConfig population.
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "../../include/config.h"

int config_load(const char *filename, SimConfig *config) {
    /* TODO: implement config file parser */
    (void)filename;
    (void)config;
    return -1;
}

void config_print(const SimConfig *config) {
    /* TODO: print all config fields */
    (void)config;
}
